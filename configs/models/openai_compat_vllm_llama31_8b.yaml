# bwUniCluster/vLLM path for meta-llama/Llama-3.1-8B-Instruct.
# base_url/api_key are read from environment variables so no secrets or
# node-specific addresses live in the scientific configuration.
#
# Served by cluster/bwunicluster3/model_profiles/llama31_8b.sh with the
# llama3_json tool-call parser and the repository-pinned Llama 3.1 JSON
# tool-use chat template (cluster/bwunicluster3/chat_templates/).
name: "Llama-3.1-8B-Instruct"
backend: "openai-compatible"
model_id: "meta-llama/Llama-3.1-8B-Instruct"
family: "llama3.1"
revision: "0e9e39f249a16976918f6564b8830bc894c89659"
dtype: "bfloat16"
device: "cuda"
served_model_name: "meta-llama/Llama-3.1-8B-Instruct"
base_url: null
base_url_env: "OPENAI_COMPATIBLE_BASE_URL"
api_key_env: "OPENAI_COMPATIBLE_API_KEY"
# Llama 3.1 has no thinking/non-thinking switch: send nothing.
reasoning_mode: "auto"
# Advisory: recorded in the manifest, must match the server launch flag.
tool_call_parser: "llama3_json"
sampling:
  temperature: 0.7
  top_p: 0.9
  max_tokens: 512
